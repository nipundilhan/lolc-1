spring:
  application:
    name: lending-product
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL}
      username: ${CONF_SERVER_USR}
      password: ${CONF_SERVER_PASS}
  profiles:
    active:
    - ${CONF_ENV_NAME}